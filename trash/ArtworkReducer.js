export default function artworkReducer(state = { artworks: [], }, action) {
    // this is where the case statements go 
    switch(action.type) {
        default:
            return state;
    }
}